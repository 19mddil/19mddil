import React, { Component } from 'react';
import Layout from './Layout';

const Cirtificates = () => (
    <div>
        <Layout title='Cirtificates' className='container'>
        </Layout>
    </div>
)

export default Cirtificates;