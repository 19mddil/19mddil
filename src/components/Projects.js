import React, { Component } from 'react';
import Layout from './Layout';

const Projects = () => (
    <div>
        <Layout title='Projects' className='container'>
        </Layout>
    </div>
)

export default Projects;